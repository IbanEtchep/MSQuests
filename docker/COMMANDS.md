# Docker Commands

start:
```shell
docker compose up -d
```

recreate:
```shell
docker compose up -d --force-recreate
```

stop:
```shell
docker compose down
```

exec:
```shell
docker compose exec -it mc rcon-cli
```